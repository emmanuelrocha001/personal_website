import 'package:flutter/material.dart';

class Configs {
  // COLORS ----------------------------------------------------------------->
  static const OFF_WHITE = Color(0xFFF5F5F5);
  // static const OFF_WHITE = Color(0xFFEFF0F3);
  static const GREY = Color(0xFF707070);
  static const LEAFY_GREEN = Color(0xFF689567);
  static const ROSE = Color(0xFFF2D3E2);
  static const APRICOT = Color(0xFFF7CAB0);
  static const BURNT_SIENNA = Color(0xFFEE6F4C);
  static const RED = Color(0xFFCD3B13);
  static const SALMON = Color(0xFFF75D5D);
  // FONT SIZE ----------------------------------------------------------------->
  static const smallFontSize = 12.0;
  static const defaultFontSize = 16.0;
  static const mediumFontSize = 20.0;
  static const largeFontSize = 24.0;
  static const xLargeFontSize = 36.0;
  static const xxLargeFontSize = 48.0;
  static const profilePicSize = 250.0;
  static const maxWidth = 600.0;

  static const defaultSpacing = 8.0;
  static const mediumtSpacing = 8.0;
  static const smallSpacing = 2.0;
  static const defaultRadius = 24.0;
}
